# Web framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# Database (async)
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0

# Validation & settings
pydantic>=2.5.0
pydantic-settings>=2.1.0
email-validator>=2.1.0  # required by pydantic EmailStr (auth request models)

# Authentication
python-jose[cryptography]>=3.3.0
bcrypt>=4.0.0  # used directly for password hashing (passlib 1.7.x is incompatible with bcrypt 5.x)

# HTTP client
httpx>=0.25.0

# Caching & task queue
redis>=5.0.0
arq>=0.25.0

# AI
anthropic>=0.35.0

# Payments
stripe>=7.0.0

# Resilience
tenacity>=8.2.0

# Structured logging
structlog>=23.3.0

# Encryption
cryptography>=41.0.0

# Form / multipart parsing
python-multipart>=0.0.6
